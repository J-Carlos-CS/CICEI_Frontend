import { createContext } from "react";
const ProjectContext = createContext(null);
const ActivityContext = createContext(null);
const ProductContext = createContext(null);
const UserProjectContext = createContext(null);
const DiffusionContext = createContext(null);
export { ProjectContext, ActivityContext, ProductContext, UserProjectContext,DiffusionContext };
